[
  {
    "id": "ts-vibe-001",
    "language": "typescript",
    "category": "vibe",
    "difficulty": "medium",
    "prompt": "AI generated. What's the TS smell?",
    "code": "async function fetchUser(id: string): Promise<any> {\n  const res = await fetch(`/users/${id}`);\n  return res.json();\n}",
    "choices": [
      { "id": "a", "text": "Return type should be Promise<User>, not Promise<any>" },
      { "id": "b", "text": "Template literals are invalid in fetch" },
      { "id": "c", "text": "Promise cannot wrap any" },
      { "id": "d", "text": "fetch doesn't exist in Node" }
    ],
    "correctChoiceId": "a",
    "explanation": "`Promise<any>` defeats the purpose of TypeScript — callers lose autocomplete and safety. Declare a `User` interface and return `Promise<User>`.",
    "tags": ["any", "return-types"]
  },
  {
    "id": "ts-vibe-002",
    "language": "typescript",
    "category": "vibe",
    "difficulty": "hard",
    "prompt": "What runtime problem hides behind this AI snippet?",
    "code": "function parseConfig(raw: string) {\n  return JSON.parse(raw) as Config;\n}",
    "choices": [
      { "id": "a", "text": "`as Config` is a cast — raw malformed JSON silently becomes typed Config" },
      { "id": "b", "text": "JSON.parse can't take a string" },
      { "id": "c", "text": "Config must be declared with interface" },
      { "id": "d", "text": "parseConfig should be async" }
    ],
    "correctChoiceId": "a",
    "explanation": "Type assertions are a compile-time lie. Parsing arbitrary strings and asserting them as `Config` can introduce any shape at runtime. Validate with Zod or hand-rolled runtime checks.",
    "tags": ["type-assertion", "runtime-safety"]
  }
]
